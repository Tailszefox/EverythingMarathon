<?php
/*
include('ip.php5');
include('language.php5');

$numericId = checkVotant($id, $ip, $language);

$_POST = protect($_POST);

$voted = getVoted($_POST['id'], $numericId);

if($voted == false)
	InsertQuery('INSERT INTO votes(id_votant, id_jeu, timestamp) VALUES("'.$numericId.'", "'.$_POST['id'].'", "'.fromUnix(time()).'")');
else
	echo 'no.';
*/
?>
